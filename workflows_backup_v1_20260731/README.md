# 워크플로우 JSON 넣는 곳

이 폴더에 ComfyUI 화면에서 **Workflow > Export (API)** 로 내보낸 `*_api.json` 파일을 넣으세요.

예:
- `wan22_i2v_api.json` — Wan2.2 이미지→영상 워크플로우
- `vidu_i2v_api.json` — Vidu 이미지→영상 워크플로우

⚠️ 일반 저장(Ctrl+S)으로 받은 JSON과는 포맷이 다릅니다. 반드시 **API용으로 내보낸 JSON**을 사용해야 `generate.py`가 정상 동작합니다.
